// Shared entry for the store country-index pages (coupons, flash sales,
// banners). Pure static links to per-country lists — no JS behaviour is
// needed, this only ships the shared grid/card CSS.
import '@tenant-css/pages/store-country-index.css';
