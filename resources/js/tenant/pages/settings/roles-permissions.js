// Roles & permissions page — the datatable, create/edit modal, and the
// per-module checkbox-group "select all" toggles are all handled by the
// core `[data-checkbox-group]` component (one instance per module group);
// nothing bespoke is needed here.
