// Wallet page — Renew/Upgrade both open the same shared payment modal
// (#subscription-payment), which always lists every published package with
// the tenant's current plan pre-selected. All behaviour beyond that is
// declarative via x-tenant:: components (datatable, payment modal).
