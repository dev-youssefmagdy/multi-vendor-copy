import '@tenant-css/pages/requests.css';

// New product request form — all behaviour is declarative via x-tenant:: components
// (x-tenant::form + x-tenant::dropzone).

// Request form design (shared with the other request pages).
import '@tenant-css/pages/dashboard.css';
import '@tenant-css/pages/orders.css';
