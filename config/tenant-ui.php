<?php

declare(strict_types=1);

return [
    /*
    |--------------------------------------------------------------------------
    | Status badge colour overrides
    |--------------------------------------------------------------------------
    |
    | Keyed by the enum's short class name, then by the backed value.
    | Consulted by <x-tenant::status-badge> when the enum has no color()
    | method of its own.
    |
    */
    'status_colors' => [
        //
    ],
];
